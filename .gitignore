# Archivos de sesión y estado local de Claude Code
sessions/
*.jsonl
.last-cleanup
ide/

# Credenciales y tokens OAuth (nunca sincronizar)
.credentials.json
.claude.json
backups/
mcp-needs-auth-cache.json

# Datos operacionales de subagentes y tool-results (no necesarios en git)
project/*/subagents/
project/*/tool-results/
